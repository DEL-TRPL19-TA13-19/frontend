const debug = import.meta.env.MODE !== "production";
// export const store = createStore({
//   modules: {
//     auth,
//   },
//   plugins: debug ? [createLogger()] : [],
//   strict: debug,
// });
